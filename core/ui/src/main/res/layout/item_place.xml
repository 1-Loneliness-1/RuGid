<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="210dp"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:background="@drawable/card_bg">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/ivPlaceCover"
            android:layout_width="match_parent"
            android:layout_height="148dp"
            android:scaleType="centerCrop"
            tools:src="@drawable/place_placeholder" />

        <ImageView
            android:id="@+id/ivFavoritePlace"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:layout_gravity="end"
            android:scaleType="centerInside"
            android:src="@drawable/bookmark"
            android:background="@drawable/bookmark_bg" />

    </FrameLayout>

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tvPlaceTitle"
        style="@style/Headline3"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginHorizontal="10dp"
        android:ellipsize="end"
        android:maxLines="2"
        tools:text="Ботанический сад Петра Великого" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tvPlaceCategory"
        style="@style/Headline4"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:layout_marginBottom="10dp"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="56dp"
        android:textColor="@color/black_font_secondary"
        android:maxLines="1"
        android:ellipsize="end"
        tools:text="Погулять" />

</LinearLayout>